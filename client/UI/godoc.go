// Package UI Contains all TUI interface components
// almost all the UI is built using tview and tcell
// functions are not exported because they are only used in the UI package
// For this reason, the package is not documented and no tests are written for it
package UI